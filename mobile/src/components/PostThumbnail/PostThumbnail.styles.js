import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{flex: 1 / 3, height: 130, margin: 2},
    thumbnail: {height: '100%', width: '100%', resizeMode: 'stretch'},
    multipleImageIcon:{position: 'absolute', right: 5, top: 10}
})

export default styles