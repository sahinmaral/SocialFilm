import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {justifyContent: 'flex-end', height: 50, flexDirection: 'row'},
  button: {
    container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    },
  },
});

export default styles;
